/**
 * Pilot credential defaults.
 *
 * The shared default password is accepted only for the 9-person pilot with
 * in-person credential handoff. v1 must use per-user temporary passwords
 * generated at account creation — do not carry this pattern forward.
 */
export const PILOT_DEFAULT_PASSWORD = 'm3m@12345';

export const BCRYPT_ROUNDS = 12;
